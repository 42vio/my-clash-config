# Placeholder-only environment template for docker compose.
# Copy to .env only to override the project name; every private value
# belongs in the ignored private/config/ tree on the host.
COMPOSE_PROJECT_NAME=clash-sub
