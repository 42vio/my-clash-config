# Parent directory of the clash-sub metadata socket: traversable by
# nginx (www-data), listable by no one else.  The installer must deploy
# this file to /etc/tmpfiles.d/clash-sub-metadata.conf; tmpfiles-setup
# creates the directory before sockets.target binds the socket.
d /run/clash-sub 0750 root www-data -
